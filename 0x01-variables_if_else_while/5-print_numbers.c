#include <stdio.h>

/**
 * main - prints out 0 - 9
 * followed by a new line
 * Return: Aways 0 (Sucess)
 */

int main(void)
{
	ch = 0;

	while (ch < 10)
	{
		printf("%d\n", ch);
		ch++;

	}

	return (0);


}
